engine-strict=true

# The same module layout everywhere. A machine that sets node-linker=hoisted
# globally would let a package import something it never declared: it resolves
# from the hoisted root locally and fails in CI, which is how
# @playwright/test slipped through once.
node-linker=isolated
